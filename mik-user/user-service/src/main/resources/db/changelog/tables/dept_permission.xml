<?xml version="1.0" encoding="utf-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
    http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.4.xsd">

    <changeSet id="#operation@create#table@dept_permission#time@2024-01-01" author="mik" labels="mik">
        <preConditions onFail="MARK_RAN" onErrorMessage="表dept_permission已存在,不需要创建">
            <not>
                <tableExists tableName="dept_permission"/>
            </not>
        </preConditions>
        <sql>
            CREATE TABLE `dept_permission`  (
                                               `id` bigint NOT NULL AUTO_INCREMENT,
                                               `dept_id` bigint NOT NULL COMMENT '部门ID',
                                               `p_id` bigint NOT NULL COMMENT '权限ID',
                                               `create_time` datetime NULL DEFAULT NULL,
                                               `update_time` datetime NULL DEFAULT NULL,
                                               PRIMARY KEY (`id`) USING BTREE,
                                               KEY `idx_dept_id` (`dept_id`)
            ) ENGINE = InnoDB;
        </sql>
    </changeSet>
</databaseChangeLog>
