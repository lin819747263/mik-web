<?xml version="1.0" encoding="utf-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
    http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.4.xsd">

    <changeSet id="#operation@create#table@role_permission#time@2018-12-20" author="linguoshuai" labels="mik">
        <preConditions onFail="MARK_RAN" onErrorMessage="表role_permission已存在,不需要创建">
            <not>
                <tableExists tableName="role_permission"/>
            </not>
        </preConditions>
        <sql>
            CREATE TABLE `role_permission`  (
                                                `id` bigint NOT NULL AUTO_INCREMENT,
                                                `role_id` bigint NOT NULL,
                                                `p_id` bigint NOT NULL,
                                                `create_time` datetime NULL DEFAULT NULL,
                                                `update_time` datetime NULL DEFAULT NULL,
                                                PRIMARY KEY (`id`) USING BTREE
            ) ENGINE = InnoDB AUTO_INCREMENT = 77 CHARACTER SET = utf8mb3 COLLATE = utf8mb3_general_ci ROW_FORMAT = COMPACT;
        </sql>
    </changeSet>
</databaseChangeLog>